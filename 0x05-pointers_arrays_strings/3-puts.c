#include "main.h"

/**
 * _puts - prints a string
 * @str: string
 * Return: success
 */

void _puts(char *str)
{
	int i = 0;
	while (s[i])
		i++;

	while (i--)
	{
		_putchar(s[i]);
	}

	_putchar('\n');
}
